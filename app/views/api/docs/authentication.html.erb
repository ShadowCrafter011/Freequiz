<%= render layout: "api_navbar" do %>
    <div>
        <h1 class="text-start">User authentication</h1>
        For requests which require a valid user access token the Authorization header must be set to an access token. Access tokens are obtained on <%= link_to "creation", api_docs_users_path(anchor: "create") %>, <%= link_to "login", api_docs_users_path(anchor: "login") %> and <%= link_to "refresh", api_docs_users_path(anchor: "refresh") %>
    </div>
<% end %>
